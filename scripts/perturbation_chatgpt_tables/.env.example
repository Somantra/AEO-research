# Copy this file to .env and fill in your own key -- only needed if you run
# perturbation_chatgpt_brand_position.py with --score-embeddings.
# Never commit a real .env file (it's excluded via .gitignore at the repo root).

OPENAI_API_KEY=
OPENAI_EMBEDDING_MODEL=text-embedding-3-small
